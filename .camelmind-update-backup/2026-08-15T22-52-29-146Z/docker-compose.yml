services:
  camelmind:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - path: .env
        required: false
    # Uncomment to edit docs without rebuilding the image:
    # volumes:
    #   - ./content:/app/content
    #   - ./nav:/app/nav
